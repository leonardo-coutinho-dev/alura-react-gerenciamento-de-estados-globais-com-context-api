import { Box } from "@mui/material";
import React from "react";

const Cart = () => {
  return (
    <Box
      sx={{
        maxWidth: "796px",
        margin: "16px auto",
        padding: "16px",
        display: "flex",
        flexDirection: "column",
        backgroundColor: "#ebebeb",
        color: "#1d1d1d",
        borderRadius: "4px",
      }}
    >
      Cart
    </Box>
  );
};

export default Cart;
